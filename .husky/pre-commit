#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged to check staged files
npx lint-staged

# Run security audit on dependencies (non-blocking)
npm audit --audit-level=moderate || true

